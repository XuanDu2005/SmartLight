# Migrations directory.

Migrations are generated by Prisma Migrate on first run.

```bash
pnpm --filter @smartlight/api exec prisma migrate dev --name init
```

Files here are created by the CLI; do not author them by hand before that first run.
