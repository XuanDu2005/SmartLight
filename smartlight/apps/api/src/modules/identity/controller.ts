import { Controller } from '@nestjs/common';

/**
 * Identity bounded context.
 * Bootstrap stub. Auth flows (register, login, refresh, MFA) are not yet wired.
 */
@Controller()
export class IdentityController {}
